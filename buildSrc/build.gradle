apply plugin: 'groovy'

repositories {
  google()
  jcenter()
}

dependencies {
  implementation gradleApi()
  implementation localGroovy()

  testImplementation 'junit:junit:4.12'
  testImplementation('com.squareup.assertj:assertj-android:+') { exclude module: 'support-annotations' }
}