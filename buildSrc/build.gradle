apply plugin: 'groovy'

repositories {
  google()
  jcenter()
}

dependencies {
  implementation gradleApi()
  implementation localGroovy()

  testImplementation 'junit:junit:4.12'
  testImplementation('com.squareup.assertj:assertj-android:1.2.0') { exclude module: 'support-annotations' }
}